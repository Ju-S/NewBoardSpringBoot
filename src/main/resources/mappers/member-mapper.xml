<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
    <select id="login" resultType="com.kedu.newboard.dto.MemberDTO">
        SELECT * FROM member WHERE id = #{id} AND pw = #{pw}
    </select>
    <select id="getById" resultType="com.kedu.newboard.dto.MemberDTO">
        SELECT * FROM member WHERE id = #{id}
    </select>

    <insert id="register">
        INSERT INTO member (
        id, pw, name, phone, email, postcode, address1, address2
        ) VALUES (
        #{id}, #{pw}, #{name}, #{phone}, #{email}, #{postcode}, #{address1}, #{address2}
        )
    </insert>

    <update id="updateById">
        UPDATE member SET
        name = #{name},
        phone = #{phone},
        email = #{email},
        postcode = #{postcode},
        address1 = #{address1},
        address2 = #{address2}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM member WHERE id = #{id}
    </delete>

</mapper>