<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Reply">
    <select id="getList" resultType="com.kedu.newboard.dto.ReplyDTO">
        SELECT * FROM reply WHERE board_id = #{boardId} ORDER BY create_at
    </select>
    <select id="getById" resultType="com.kedu.newboard.dto.ReplyDTO">
        SELECT * FROM reply WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO reply(id, writer, board_id, contents)
        VALUES(reply_seq.nextval, #{writer}, #{boardId}, #{contents})
    </insert>

    <update id="updateById">
        UPDATE reply SET contents = #{contents} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM reply WHERE id = #{id}
    </delete>
</mapper>